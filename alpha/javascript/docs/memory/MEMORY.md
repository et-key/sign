- [Implementation collaboration style](feedback_implementation_style.md) — verify empirically, propose spec edits before writing, stay minimal, self-correct openly.
- [Sign's origin and direction](project_sign_origin_and_direction.md) — LLVM-era grievance → category theory → AArch64 as an algebra-carrier → OS/kernel + the natural-language/formal-methods pivot. motivation.md is 建前, don't cite it.
- [Sign project status](project_sign_status.md) — the type system is now the active thread; pre-flight audit's group A fixed, pass3 still not wired into the execution path.
- [Pre-type-system behavior audit](project_sign_pre_type_system_audit.md) — 6 silently-wrong bugs fixed (Layer-1 category, `#`-export binding, `[]`, chain compare, `!__`=Id射, `!=`); groups B/C still open.
- [Type system is live](project_sign_type_system_build.md) — compile.js driver wired pass1b+pass3 in, evaluator reads node.atomType, Layer 2 gained List(T). Settled promotion/division/index/String-absorption semantics.
- [Undefined-identifier diagnostic severity](project_sign_diagnostic_severity.md) — settled as "information" not warning, except tail-position (tco.md).
- [apply_reverse design (UFCS not SVO)](project_sign_apply_reverse_design.md) — `x f` is receiver-piping only, Option A (infix SVO) rejected; pass2.js tier-10 bug fixed.
- [Guide-example audit findings](project_sign_bracket_destructuring.md) — all originally-flagged items now done (destructuring/match_case/push/unshift/range/pointfree/neg-index/`~` flatten). Pointfree comparisons deliberately diverge from evalCompare's §4 0/1-swap rule.
- [Operator table audit](project_sign_operator_table_audit.md) — fixed List-List-no-tilde, postfix-@ precedence, `!=` eval bug, range-family (all 5 ops), `==`/`!==` (structural (in)equality); `><` reverse removed (§2.5 destructuring replaces it). `===` eval still open.
- [Sign's type philosophy](project_sign_type_philosophy.md) — core thesis: structural/trait-based types, no nominal "name as truth". Now canonical as 0_design_principles.md 原理6 (product+sum both emerge from `==`). Pass1b multi-arg bug fixed. Keep spec docs non-critical of prior art.
- [8-Queens real-program audit](project_sign_8queens_audit.md) — 8 real bugs fixed & committed (Unit-filter, scalar-as-1-list, mixed bracket-params, neg-index, `~` flatten, isUnit([]), get-rest). Original clean recursive design now runs N=1..8 correctly, nothing left open.
- [Currying/type-system design](project_sign_currying_design.md) — $/@/# (1651c84) AND automatic currying (10f0777, static pass2 arity-shortfall detection) both implemented now. Only the shape/layout pass (Struct field addressing) remains.
- [Return-value addressing design](project_sign_return_value_addressing_design.md) — String/List returns are implicit references (list-model isomorphism); proposed fix is sret-style (caller provides slot, callee writes via `#`). Design-only, no alpha/javascript angle (JS's GC already makes it a non-issue).
- [string_and_comment.md audit](project_sign_string_comment_audit.md) — 3 bugs fixed: empty-string≅Unit, string indexing as chars, and a grammar bug where code+comment couldn't coexist in one file (plus a hidden PEG-non-backtracking bug inside `comment` itself). No inline comments is deliberate design, not a gap.
- [Tail-call optimization](project_sign_tco.md) — trampoline in applyClosure/evaluateTail (commit 4f5cca6). Sign has no loops, only recursion, so this was high-priority: 2000-deep mutual recursion crashed before, now 1M+ deep runs in ~560ms. Non-tail recursion still correctly bounded.
