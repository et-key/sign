{
	"name": "sign-alpha-js",
	"version": "0.1.0",
	"description": "Sign言語 lexer/parser 再実装（alpha, JavaScript版）",
	"type": "module",
	"scripts": {
		"build:parser": "pegjs -o parser.js sign.pegjs",
		"test": "node test/run.js"
	},
	"dependencies": {
		"pegjs": "^0.10.0"
	}
}
