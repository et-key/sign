# Sign言語演算子記号表（優先順位順）

## 基本原則

- 前置演算子は、対象値の直前に配置しなければならない（対象値との間を空白で区切ってはならない）
- 後置演算子は、対象値の直後に配置しなければならない（対象値との間を空白で区切ってはならない）
- 中置演算子は、対象値の間に配置し、空白で区切らなければならない
- 予約語なしで演算子のみで表現
- 定義演算子による識別子の宣言は、同時に型を生成する
- 記号の自然な意味と操作的意味の一致
- 優先順位の低い順（後から評価される順）から高い順に配列
- 余積演算子は、単なる区切り文字として考えてもよく、全ての空白を余積演算子と見なせる
- 空白を区切りとみなしても良い理由は、後段の処理で積演算子との優先順位を適宜決定可能であるため
- 改行を演算子とみなす事もでき、その場合の機能は、行単位での評価としての意味をもつ
- 絶対値囲みの開始の後は空白なし
- 絶対値囲みの終了の前は空白なし
- かっこ囲みの開始の後は空白なし
- かっこ囲みの終了の前は空白なし
- 持ち上げる（生成して展開する）演算子と持ち下げる（畳み込む）演算子の関係性が、包括的に型を決定する
- 持ち下げる（畳み込み）の演算は、左辺の型へキャストする
- 余積はコード生成前に対応する型付けが可能であるため、この言語は静的型付け言語である
- **未定義識別子の「無（Unit）」への収束**:  
定義されていない識別子はすべて `__`（空の余積・無）として評価される。予約語を用いずに仮想キーワードやDSL的コードを記述するための言語の根幹設計である
- **Unitの対象吸収と非対称性については、演算子テーブルに記載のとおり**
- **`__`（即時崩壊）と `$__`（遅延サスペンド）の明確な区別**:
  - `__` は関数適用の吸収元であり、式全体を即座に崩壊させる
  - `$__` は `__` のアドレス（参照）であり、これは有効な値（非Unit）として扱われ、完全性公理による崩壊を防ぎ、「休眠状態（Promise/Thunk）」を表現する
- **`$` の対象による振る舞いの二重性**:
  - `$匿名式`（`$[x ? x]`、`$!__` など）: その場で生成された**オブジェクト本体のアドレス**を取得する。C++ の `&(new [](x){x})` に相当。コンパイラが同一ボディをデデュープした場合、複数の定義から同じアドレスが返りうるため、一意性は保証されない
  - `$名前付き識別子`（`$Red`、`$b` など）: **binding（変数）自体のアドレス**を取得する。C++ の `&b` に相当。binding ごとに一意・安定したアドレスが保証される
  - この区別は enum バリアントの実装に直結する。`Red : !__`（`!__` = id射）と定義した場合、`$Red` は「Red という named binding のアドレス」であり、同じ `!__` を持つ `$Green` とは必ず異なる。一方 `Red : $!__` とすると匿名 id 射のアドレスになり、コンパイラのデデュープにより `$Green` と同一アドレスになりうる。したがって、**enum バリアントは `Red : !__`（named Lambda）として定義しなければならない**
- **型コンストラクタとなる識別子は、大文字で始めること。**:
  - 自分で型を定義する際、大文字で始まる関数名はコンパイラ内部でその関数が返す値の型として認識する
  - 大文字で開始する識別子で、struct、enum、コンストラクタ関数を定義可能

## 全演算子一覧表

- 右単位元…右結合は位置表記内に※あり
- 前置※、または後置で優先順位を必ず重視しなくてはならないものは、優先順位表記内に※あり

位置だけで不十分な場合、型の組み合わせも記述する

### Unit 欄の読み方（射と継続）

Unit 欄は、演算子が `__` を見たときに**どの射へ落ちるか**を宣言する欄である。
Sign において真は Id射（恒等射）、偽は `__`（零対象）であり
（[`categorical_truth.md`](../appendix/categorical_truth.md)）、この欄はそのどちらへ
合成されるかを述べている。「吸収元／単位元」という代数の語ではなく射の語で書くのは、
**結果**と**継続**という2つの軸が代数の語では区別できないためである。

- **零射** … 結果は `__`。以降どう合成しても `__` のまま
- **恒等射（値）** … 括弧内の値がそのまま結果として通る
- **`/`** … その位置に被演算子を取らない

> [!IMPORTANT]
> **継続の規則**：**左辺の評価だけで結果が確定する場合、右辺は評価されない。**
>
> - 左辺が零射へ落ちる演算子（算術・`'`・`&` など）は、その時点で結果が `__` に確定する。
>   零射との合成は零であり、右辺の値は結果に寄与しえないため、右辺を評価してはならない
> - `|` のように左辺が非Unitであることで結果が確定する演算子も、同じ理由で右辺を評価しない
>
> `|`／`&` の「短絡評価」はこの規則の別名であって特例ではない。
>
> Sign は副作用（`#` による書き込み、MMIO）と非停止を持つため、右辺を評価するか否かは
> **観測可能**である。`__ < ($UART # x)` で書き込みが起きるかどうかがこの規則で決まる以上、
> 「結果が `__` になる」ことだけを定めた仕様は不完全であり、実装ごとに挙動が割れる。
> したがってこの規則は規範的であり、零射へ落ちながら右辺を評価する実装は誤りである。

| 優先順位 | 記号 | 位置（型の組み合わせ） | 機能 | 自然な意味 | 操作的意味論 | 左辺がUnit | 右辺がUnit |
| :---------: | :------: | :------: | :------: | ----------- | ------------- | ---------- | ---------- |
| 1 | `#` | 前置※ | export | ハッシュタグ（公開・発見可能） | 名前をプロジェクト内部から発見可能にする | / | Unitをexportする |
| 1 | `##` | 前置※ | export | ハッシュタグ（公開・発見可能） | 名前を外部から発見可能にする（ARCに対応） | / | Unitをexportする |
| 1 | `###` | 前置※ | export | ハッシュタグ（公開・発見可能） | 名前を外部から発見可能にする （Pin領域でunloadしない前提） | / | Unitをexportする |
| 2 | `:` | 中置※ | define | 即ち（AならばB） | 左辺の名前や条件を、右辺の式に束縛する | 零射 | identifier : __ は定義可能 |
| 3 | `?` | 中置※ | lambda | 問いかけ（どうするか？） | 関数定義 | 引数を取らない関数 | Unitを返す関数を定義できる |
| 4 | `#` | 中置※ | output | ハッシュタグ（関連付け） | アドレスにデータを入れ、成功したらアドレスを返す | Unitを返す | 0x00 # __ は、何もしないがアドレス値は返ってくる |
| 5 | `;` | 中置 | xor | 排他的関係 | 排他的論理和 | 恒等射（右辺値 X） | 恒等射（左辺値 X） |
| 6 | `\|` | 中置 | or | または（通路） | 論理和（短絡評価） | 恒等射（右辺値 X） | 恒等射（左辺値 X） |
| 7 | `&` | 中置 | and | かつ（結合） | 論理積（短絡評価） | 零射 | 零射 |
| 8 | `===` | 中置 | same | 同一性 | 同一性比較演算 | 零射 ※1 | 零射 |
| 8 | `==` | 中置 | equal | 等しい | 構造内比較演算 | 零射 ※1 | 零射 |
| 8 | `!==` | 中置 | xnot_equal | 等しくない | 構造内比較演算 | 恒等射（右辺値 X） | 恒等射（左辺値 X） |
| 9 | `,` | 中置※ | product | 積（構造的組み立て） | 右結合なリスト構築 | 恒等射 ※3 | 恒等射 ※3 |
| 10.0 | ` ` | `Atom \| List \| Struct` 中置 `Atom \| List \| Struct` | construct | 並べる（連接） | 構造構築 | 恒等射 | 恒等射 |
| 10.1 | ` ` | `Atom \| List~` 中置 `Atom \| List~` | push<br>unshift | 並べる（連接） | リストへ追加 | 恒等射 | 恒等射 |
| 10.2 | ` ` | `List~ \| Struct~` 中置 `List~ \| Struct~` | concat | 並べる（連接） | リスト/構造体の結合 | 恒等射 | 恒等射 |
| 10.3 | ` ` | `Atom \| List \| Struct` 中置 `Lambda` | apply | 逆適用 | 関数適用 | 恒等射 | 恒等射 |
| 10.4 | ` ` | `Lambda` 中置 `Atom \| List \| Struct` | apply | 適用 | 関数適用 | 恒等射 | 恒等射 |
| 10.5 | ` ` | `Lambda` 中置 `Lambda` | compose | 左結合な関数合成 | 関数合成 | 恒等射 | 恒等射 |
| 11 | `~` | 中置 | range | around（範囲のその辺り） | 範囲リスト構築 | 零射 | 零射 |
| 11 | `~+` | 中置 | range | around（範囲のその辺り） | 等差数列指定 | 零射 | 零射 |
| 11 | `~-` | 中置 | range | around（範囲のその辺り） | 等差逆数列指定 | 零射 | 零射 |
| 11 | `~*` | 中置 | range | around（範囲のその辺り） | 等比数列指定 | 零射 | 零射 |
| 11 | `~/` | 中置 | range | around（範囲のその辺り） | 等比逆数列指定 | 零射 | 零射 |
| 11 | `~^` | 中置 | range | around（範囲のその辺り） | 等冪数列指定 | 零射 | 零射 |
| 12 | `<` | 中置 | less | より小さい | 比較演算 | 零射 ※1 | 零射 |
| 12 | `<=` | 中置 | less_equal | 以下 | 比較演算 | 零射 ※1 | 零射 |
| 12 | `=` | 中置 | equal | 等しい | 比較演算 | 零射 ※1 | 零射 |
| 12 | `>=` | 中置 | more_equal | 以上 | 比較演算 | 零射 ※1 | 零射 |
| 12 | `>` | 中置 | more | より大きい | 比較演算 | 零射 ※1 | 零射 |
| 12 | `!=` | 中置 | not_equal | 等しくない | 比較演算 | 恒等射 | 恒等射 |
| 13 | `+` | 中置 | add | 加法 | 算術演算 | 零射 | 恒等射（左辺値 X） |
| 13 | `-` | 中置 | sub | 減法 | 算術演算 | 零射 | 恒等射（左辺値 X） |
| 14 | `*` | 中置 | mul | 乗法 | 算術演算 | 零射 | 恒等射（左辺値 X） |
| 14 | `/` | 中置 | div | 除法 | 算術演算 | 零射 | 恒等射（左辺値 X） |
| 14 | `%` | 中置 | mod | 剰余 | 算術演算 | 零射 | 恒等射（左辺値 X） |
| 15 | `^` | 中置※ | pow | 冪乗 | 指数演算 | 零射 | 恒等射（左辺値 X） |
| 16 | `\|...\|` | 囲み | abs | 絶対値・要素数 | 絶対値／要素数演算 | / | オペランド型による ※4 |
| 17 | `'` | 中置 | get | 所有格（'s のs省略） | 構造から値を取得 | 零射 | 零射 |
| 17 | `@` | 中置※ | get | at（〜において） | 構造から値を取得 | 零射 | 零射 |
| 18 | `<<` | 中置 | 左ビットシフト | 左ビットシフト | 左ビットシフト | 零射 | 恒等射（左辺値 X） |
| 18 | `>>` | 中置 | 右ビットシフト | 右ビットシフト | 右ビットシフト | 零射 | 恒等射（左辺値 X） |
| 19 | `\|\|` | 中置 | bit or | ビットマスク | ビット毎の論理和 | 零射 | 恒等射（左辺値 X） |
| 20 | `;;` | 中置 | bit xor | ビットマスク | ビット毎の非交和 | 零射 | 恒等射（左辺値 X） |
| 21 | `&&` | 中置 | bit and | ビットマスク | ビット毎の論理積 | 零射 | 恒等射（左辺値 X） |
| 22 | `!` | 後置 | factorial | 階乗 | 階乗演算 | 零射 | / |
| 22 ※ | `~` | 後置 | expand | 冒頭〜 | 展開 | 零射 | / |
| 23 ※ | `~` | 前置※ | continuous | 〜末尾 | 連続リスト構築 | / | 零射 |
| 23 | `!` | 前置※ | not | 否定 | 論理否定 | / | Id射（評価予定が確定・非Unit・真）|
| 23 | `$` | 前置※ | address | お金（価値の抽象化） | アドレス取得 | / | __ のアドレスは取得できる |
| 23 | `@` | 前置※ | input | at（〜において） | アドレスからデータを取得 | / | 零射 |
| 23 | `!!` | 前置※ | bit not | ビット反転 | ビット毎の否定 | / | 零射 |
| 23 | `-` | 前置※ | negate | 符号反転（マイナス） | 算術的な符号反転（数値であることが前提のため算術演算に分類され、代数式の優先順位に従いべき乗（tier15）より優先度が高い） | / | 零射 |
| 24 | `@` | 後置 | import | at（〜から） | ファイルから取得 | 零射 | / |
| 25 | `(...)` | 囲み | block | ブロック | インラインブロック構築 | / | 零射 |
| 25 | `{...}` | 囲み | block | ブロック | インラインブロック構築 | / | 零射 |
| 25 | `[...]` | 囲み | block | ブロック | インラインブロック構築 | / | 零射 |
| 26 | `\t` | 前置 | indent | インデント | インデントブロック構築 | / | 零射 |

### 表中の ※ について（未決・実装との差異）

いずれも「射」と「継続」を分けたことで初めて可視化された箇所である。
実装は `alpha/javascript` の現状を指す。

- **※1 比較演算子の継続（未決）**：`<` `<=` `=` `>=` `>` `===` `==` は左辺が零射へ落ちるが、
  実装は右辺を評価している。上の継続の規則（零射なら右辺を評価しない）に照らすと不整合であり、
  算術（`+` など）や `'` は同じ条件で右辺を評価しない。どちらへ揃えるかが未決。
- **※2 `!=` の左辺（決着済み）**：表の通り**両辺とも恒等射**である。片方が Unit なら
  「等しくない」ことが確定して真になり、真のときに情報を運ぶのは非Unit側の値なので
  そちらを返す（`__ != 5` も `5 != __` も 5）。実装は左辺Unitのみ零射を返していたため
  引数の順序で挙動が変わっていた。`!==` は元から両辺とも非Unit側を返しており、
  そちらが正しい形だった。表側に合わせて実装を修正済み。
- **※3 `,`（直積）の Unit（未決）**：表は恒等射（消える）だが、実装は `__` を**スロットとして保持**する
  （`|[1 , __ , 2]|` が 3 になる）。余積（空白）が `__` を消す（`|1 __ 2|` が 2）のと対照的で、
  「空のコレクション」と「値の不在」がこの2演算子で既に区別されている可能性がある。
- **※4 `|...|` の Unit**：この演算子は数値の絶対値とリストの要素数を兼ねる。オペランドが Unit のとき、
  `__ = []`（[`unit.md`](../core/unit.md)）の同一視によって「空リスト＝要素数 0」とも「値の不在」とも
  読めてしまい、**値だけでは決まらない**。そのため Layer 2 の型で決める（原理2）——
  `List`／`String` なら **0**、それ以外（`Unit`・型が解決できない場合）は **零射**。
  型が付かない側を 0 に倒さないのは、不在がもっともらしい値に化けるのを防ぐためである。
  空を受け取ったうえで数値が必要な場合は、呼び出し側が明示的に絞る。

> [!NOTE]
> **比較演算子の返値ルール**（真のときに返す値）は [`Value_Based_Comparison_Chaining_ja-jp.md`](../type/Value_Based_Comparison_Chaining_ja-jp.md) を参照。
> 簡単なまとめ：左辺が Int 値 `0` または `1`（算術単位元）の場合は右辺を返し、それ以外は左辺を返す。

> [!NOTE]
> **`;`（xor）の返値について**  
> `;` は `|`（or）の短絡評価と**Unit絡みの挙動が類似している**が、両辺が非Unitのときの動作が根本的に異なる。
>
> | ケース | `\|`（or） | `;`（xor） |
> |--------|-----------|-----------|
> | 左辺 `__`、右辺 非Unit | 右辺を返す（単位元） | 右辺を返す（単位元） |
> | 左辺 非Unit、右辺 `__` | 左辺を返す（短絡評価・右辺未評価） | 左辺を返す（単位元） |
> | 両辺ともに 非Unit | **左辺を返す**（短絡評価） | **`__` を返す**（相殺） |
> | 両辺ともに `__` | `__` | `__` |
>
> **`;` は短絡評価を行わない**。両辺を必ず評価したうえで、どちらか一方だけが非Unitであればその値を返し、両辺が非Unitであれば相殺して `__` を返す。これは排他的論理和（XOR）の代数的性質（`A XOR A = false`）に対応する。

- \$__ # expr は、__ の上書きが言語を破壊してしまうため、この表現はエラーとなる。  
これは、$(0x80 < 0x40) # expr となるような表現でも同様であり、致命的なエラー（不正なアドレスへの書き込み）として扱う。  
例外発生時、この行の式全体を返す仕組みを作るべき。
- 条件分岐は関数のブロック構文によって表現されるmatch_case式か論理演算の短絡評価を使用する。
- 関数合成を右結合で行いたい場合は、カッコを使用して明示する。
- 中置`'`演算子は、(list | struct) ' index となる。
- 中置`@`演算子は、index @ (list | struct) となる。

## 特殊記号

| 記号 | 機能 | 自然な意味 | 操作的意味論 |
| :------: | :------: | ----------- | ------------- |
| `\` | 文字の指定 | 文字指定 | 直後の1文字を文字として扱う |
| `` `...` `` | 文字列 | 文字列 | 文字のリスト |
| `__` | unit | 見える無値（空の明示） | 空のリスト/恒等射/単位元 |
| `_` | hole | ホール（欠落部分） | プレースホルダとして使用 |
| `"..."` | Volatile | バックエンドコードの直接挿入 | ブロック構文のみ対応。指定ターゲットのアセンブリコードを直接挿入する。Sign変数のキャプチャ規則は未定義（余白）。 |

- **!__ は評価が予定されているid射である**:  
そのため、以下の論理的な原則を持つ。
  - !__ != __  
つまり、__ と !__ は等しくない
  - !__ !== __  
構造比較をしても、__ と !__ は等しくない
  - __ 5 == !__ 5  
余積の左単位元として__が使用されたときと、評価予定のid射であるため、双方5が返る
  - 5 __ == 5 !__  
余積の右単位元として__が使用されたときと、評価予定のid射であるため、双方5が返る

## 設計哲学

- **万人が理解できる記号**: 数学的厳密性より直感的理解を優先
- **自然言語との対応**: プログラムが文章として読める
- **予約語の排除**: 単語の意味の曖昧性を避け、記号の明確性を重視
- **メタ言語としての機能**: 任意の言語パラダイムを関数として実装可能
