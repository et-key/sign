` 1. Basic Block: stream is allocated but not escaped, so it will be freed when block exits.
f : _ ? [
  s : [0 ~+ 1]
  10
]

` 2. Escaped Block: stream is escaped using #, so it survives the block.
g : _ ? [
  s : [0 ~+ 1]
  #s
]

a : f 
b : g 
