` 1. Partial Application using Hole (_)
f : x y ? x + y
partial : f _ 3

` 2. Currying (applying one argument at a time without _)
curried : f 2
result_curried : curried 4

` 3. Conditional Maybe<x>
check : 10 < 5
check2 : 5 == 5
