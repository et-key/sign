`===========================================
` 予約語衝突テスト
` printなどのCommon Lisp予約語を使用
`===========================================

` 1. print関数の定義（予約語）
print : str ? 0x1 # str

` 2. printの使用
print `Hello, World!`

` 3. 他の予約語のテスト
list : 1, 2, 3
map : x ? x * 2
format : `test`

` 4. 予約語でない名前（変換されないはず）
myFunc : x ? x + 1
result : 42
