`===========================================
` Sign言語演算子テストコード
` 現在の実装と仕様の違いを確認するためのテストケース
`===========================================

`-------------------------------------------
` 1. Define演算子 (:) - 優先順位2
`-------------------------------------------
x : 10
y : 20
name : `hello`

`-------------------------------------------
` 2. Output演算子 (#) 中置 - 優先順位3
`-------------------------------------------
` 仕様: アドレスにデータを関連付ける
` 現在の実装: printとして変換
result : x + y
result # _

`-------------------------------------------
` 3. Export演算子 (#) 前置 - 優先順位1
`-------------------------------------------
` 仕様: 名前を外部から発見可能にする
#exported_value : 42

`-------------------------------------------
` 4. Lambda演算子 (?) - 優先順位7
`-------------------------------------------
add : x y ? x + y
square : n ? n * n
identity : x ? x

`-------------------------------------------
` 5. Product演算子 (,) - 優先順位4
`-------------------------------------------
` リスト構築
list1 : 1, 2, 3
coords : 10, 20, 30

`-------------------------------------------
` 6. Range演算子 (~) - 優先順位8
`-------------------------------------------
` 範囲指定
range1 : 1 ~ 10
range2 : 0 ~ 2 ~ 10

`-------------------------------------------
` 7. 算術演算子 - 優先順位14-16
`-------------------------------------------
` 加減算
sum : 5 + 3
diff : 10 - 4

` 乗除算
prod : 6 * 7
quot : 20 / 4
mod_result : 17 % 5

` 冪乗
power : 2 ^ 8

`-------------------------------------------
` 8. 比較演算子 - 優先順位13
`-------------------------------------------
` 各種比較
is_less : 5 < 10
is_less_eq : 5 <= 5
is_equal : 10 = 10
is_more_eq : 15 >= 10
is_more : 20 > 10
is_not_eq : 5 != 10

`-------------------------------------------
` 9. 論理演算子 - 優先順位10-12
`-------------------------------------------
` NOT (前置)
bool1 : !0
bool2 : 1

` AND
and_result : 1 & 1

` OR
or_result : 0 | 1

` XOR
xor_result : 1 ; 1

`-------------------------------------------
` 10. Get演算子 (') - 優先順位21
`-------------------------------------------
` 構造から値を取得
` obj'field のような形式

`-------------------------------------------
` 11. Address/Input演算子 ($, @) - 優先順位20, 22
`-------------------------------------------
` Address取得 (前置$)
` Input取得 (前置@)

`-------------------------------------------
` 12. ポイントフリー記法
`-------------------------------------------
` DirectFold: 演算子のみ
fold_add : [+]

` 右部分適用: 演算子 + 値
add_one : [+ 1]

` 左部分適用: 値 + 演算子
sub_from_10 : [10 -]

` map用: カンマ付き
double_all : [* 2,]

`-------------------------------------------
` 13. 複合的な式
`-------------------------------------------
` 優先順位の確認
complex1 : 2 + 3 * 4
complex2 : 10 - 2 ^ 3
complex3 : 1 + 2, 3 + 4

` ラムダと演算
calc : a b ? a * 2 + b * 3

` ネストしたブロック
nested : (1 + 2) * (3 + 4)

`-------------------------------------------
` 14. 階乗 (!) 後置 - 優先順位17
`-------------------------------------------
` 注意: 前置NOTと後置factorialの区別

`-------------------------------------------
` 15. 絶対値 (|...|) - 優先順位18
`-------------------------------------------
` 囲み演算子

`-------------------------------------------
` 16. Expand (~) 後置 - 優先順位19
`-------------------------------------------
` リストの展開

`-------------------------------------------
` 17. Continuous (~) 前置 - 優先順位9
`-------------------------------------------
` 連続パラメータ（ラムダ内で使用）
variadic : x ~rest ? x
