` テスト: 関数定義と呼び出し

` 1. 引数付き関数の定義 (Definition with parameters)
` add : x y ? x + y
add : x y ? x + y

` 2. 関数呼び出し (Function Call)
` result should be 30
result : add 10 20

` 3. ネストされた呼び出し
` result_nested should be 50
result_nested : add (add 10 20) 20

` 4. 定数関数 (既存機能の回帰テスト)
answer : 42
